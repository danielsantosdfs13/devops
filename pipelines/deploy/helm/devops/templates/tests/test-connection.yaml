apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops.fullname" . }}-test-connection"
  labels:
    {{- include "devops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
